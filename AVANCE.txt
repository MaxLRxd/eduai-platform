EDUAI PLATFORM — AVANCE DEL PROYECTO
=====================================
Fecha: 2026-09-06

STACK
-----
- Frontend: React 18 + TypeScript + Vite + Tailwind + @tanstack/react-query
- Backend: Node + Express + TypeScript + Prisma (PostgreSQL) + JWT (access/refresh)
- Servicios IA: carpeta ai-services (Python) — integración parcial

ESTADO: CONECTADO AL BACKEND REAL
---------------------------------
- Autenticación + refresh token + seed de datos (admin/docente/alumno)
- Dashboard (alumno, docente, admin)
- Docente: cursos, estudiantes, calificaciones
- Docente: mensajes
- Docente: asistencias
- Docente: correcciones
- Alumno: entregas
- Alumno: contenido de las materias
- Alumno: enrolamiento a materias (claves)
- Docente: plan de materia
- Analytics: conexión con backend

ADMIN — CONECTADO AL BACKEND REAL
---------------------------------
- Admin Usuarios: listado, alta, activar/desactivar, cambio de rol — endpoints /api/admin/users (GET/POST/PATCH)
- Admin Materias: listado, alta, edición, asignación de docente — /api/admin/materias (GET/POST/PUT) y /:id/profesores
  * Fix: se agregó profesor_id al DTO de usuario (la asignación usa la tabla Profesor, no el id de usuario)
- Admin Claves de matriculación: listado con inscriptos, generación y revocación
  * Endpoints nuevos: GET/POST /api/admin/enrollment-keys + PATCH /:id/revocar
- Admin Ajustes/Branding: GET/PUT /api/admin/branding — nombre y colores de la institución
  * Presets de color siguen siendo estáticos de la UI (no viven en backend)
- Admin Reportes: GET /api/admin/reports — 6 reportes con datos reales (asistencia, notas, tutor IA, retención, MAU, resumen ejecutivo)
  * Exportación CSV real por reporte: GET /api/admin/reports/:type/export

FALTA / PENDIENTE
-----------------
ADMIN:
- Licencias: sin backend (depende de proveedor de billing, no definido)
- Subida de logo/escudo: el `logo_url` está en el modelo, pero no hay upload de archivos todavía
- Reportes: la exportación es CSV; un export PDF real requeriría un generador de PDF

ALUMNO:
- Progress / Analytics del alumno: service mock, depende del Analytics Engine

IA:
- Tutor (ask_tutor) y herramientas del docente (generate_teaching_material) mockeados — apuntan a ai-service
- ai-services: correct_submission no conectada (TODO en aiClient.ts)

PRÓXIMOS PASOS SUGERIDOS
------------------------
1. Commitear Reportes (backend + frontend listos)
2. Subida de logo/escudo para la institución
3. Integrar ai-service: tutor, generación de material, corrección
4. Evaluar módulo de Licencias

CÓMO CORRER
-----------
- Backend: cd backend && npm run dev (puerto 3000, requiere Postgres con prisma migrate/seed)
- Frontend: cd frontend && npm run dev (VITE_API_URL por defecto http://localhost:3000)
- Verificación de tipos: frontend npm run typecheck | backend npx tsc --noEmit